import { AddressMap } from './from-json';

export const columbus5: AddressMap = {
  whiteWhaleToken: '',
  loopToken: '',
  nexusToken: '',
};

export const bombay13: AddressMap = {
  whiteWhaleToken: '',
  loopToken: '',
  nexusToken: '',
};
